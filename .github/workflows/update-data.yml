name: Update CSV from Google Sheet
on:
  workflow_dispatch:   # Manuelles Auslösen
  schedule:
    - cron: "0 6-21 * * *"  # Jede Stunde von 7:00 bis 22:00 MEZ

permissions:
  contents: write      # Schreibrechte für Commits

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Download Google Sheet as CSV
        run: |
          curl -L "https://docs.google.com/spreadsheets/d/1_shdcAB2-cR7D0nSyBSx1c6kU15-g9GvI9XbJK4HuGY/export?format=csv&gid=0" -o data.csv

      - name: Debug CSV-Inhalt
        run: cat data.csv  # Zeigt den CSV-Inhalt im Log

      - name: Commit CSV
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Update data.csv"
          files: data.csv
